// Shared by popup.js and offscreen.js (loaded as a plain <script> before
// them, so this just defines a global). Tried in order; the first host with
// a valid session wins.
const API_BASES = ["http://localhost:3000", "https://capca-cam.vercel.app"];
